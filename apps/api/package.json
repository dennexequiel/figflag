{
  "name": "@figflag/api",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "^4.10.2",
    "@figflag/shared": "workspace:*"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}
